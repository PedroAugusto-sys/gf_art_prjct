# Copie para .env (local) ou configure no Netlify (Site settings → Environment variables).
# Opcional, mas recomendado: sem gameId o Playroom pode limitar DAU.
# Obtenha em https://docs.joinplayroom.com/errors/no-game-id
VITE_PLAYROOM_GAME_ID=

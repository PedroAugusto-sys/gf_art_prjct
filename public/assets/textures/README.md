# Texturas e materiais

Coloque aqui texturas auxiliares (piso, parede, skybox/HDRI, etc.).

Exemplos de uso futuro:
- Skybox/ambiente: um arquivo `.hdr` carregado com `<Environment files="/assets/textures/venice_sunset.hdr" />` do drei.
- Materiais PBR: mapas de cor/normal/rugosidade aplicados via `useTexture` do drei.
